UnDo
3   return x < min and min or (x > max and max or x)
g@Cm
g@Ck
return M
g@Cl
3   return x < min and min or (x > max and max or x)
   if increment then
/      return M.round(x / increment) * increment
   end
>   return x >= 0 and math.floor(x + 0.5) or math.ceil(x - 0.5)5
