UnDo
t7$V>X
    enable = true;
{ config,}5
{ config, pkgs,}5
{ config, pkgs, .}5
{ config, pkgs, ..}5
{ config, pkgs, ...}5
{ config, pkgs, ... }5
  environment.5
{ config, pkgs, ... }: {5
  {5
    
  environment5
    sy5
    systemPackages = wit5
    systemPackages = with pk5
  }5
    }5
  }5
     systemPackages = with pkgs {5
    systemPackages = with pkg;{5
     systemPackages = with pkg; {5
!    systemPackages = with pkg; []5
    };5
     systemPackages = with pkg; [5
      
      
      
      
    
      pa5
      packages = wi5
      packages = with p5
      
      packages = with pkgs;5
      net5
      nerd-fonts.5
      nerd-fonts.fira5
      nerd-fonts.5
      nerd-fonts.droi5
    ]5
    ];5
    }5
    
      
    nix.5
    }5
      }5
    }5
        au5
        
        user = "matthewh"5
        user = "matthewholden"5
      
      }5
(        experimental-features = "nix-co"5
4        experimental-features = "nix-command flakes"5
        
        war5
      
      
    
        all5
        allowUnfree = true;5
    }5
    };5
    }5
      
      hostPlatform = "aarc"5
%      hostPlatform = "aarch64-darwin"5
    
  };5
    fonts = {
      packages = with pkgs; [
      nerd-fonts.fira-code
       nerd-fonts.droid-sans-mono
      ];
    };5
    nix = {
      gc = {
        automatic = true;
        user = "matthewholden";
      };
      settings = {
5        experimental-features = "nix-command flakes";
        warn-dirty = false;
      };
    };
    nixpkgs = {
&      hostPlatform = "aarch64-darwin";
      config = {
        allowUnfree = true;
'        allowUnsupportedSystem = false;
      };
    };5
  pro5
  programs.5
  programs.zsh.5
  programs.zsh.en5
  ser5
  services.5
  services.nix-d5
  services.nix-daemon.5
  services.nix-daemon.e5
  ser5
  security.5
  security.pam.5
  security.pam.enab5
{ config, pkgs, ... }: 5
  us
    users.users = {5
  networking.5
  networking.hostname = ""5
$  services.nix-daemon.enable = true;5
      user = "matthewholden";5
    
    enable5
    enable = true;5
