UnDo
v{L^!
#!/usr/bin/env lua
#!/usr/bin/env lua5
#!.5
#!/opt.5
#!/opt/ho5
#!/opt/homebrew/b5
#!/opt/homebrew/bin/lua5
 -- Require the sketchybar module
sbar = require("sketchybar")
J-- Set the bar name, if you are using another bar instance than sketchybar
"-- sbar.set_bar_name("bottom_bar")
N-- Bundle the entire initial configuration into a single message to sketchybar
sbar.begin_config()
>-- Load the sketchybar-package and prepare the helper binaries5
sbar.hotload(true)
sbar.end_config()
M-- Run the event loop of the sketchybar module (without this there will be no
1-- callback functions executed in the lua module)
sbar.event_loop()5
require("init")
